function makeCounter(initialValue = 0) {
    // throw 'Not implemented';
    // console.log(initialValue)
    // return (initialValue) => {
    //     console.log('return initial value: ', initialValue)
    // }
    return (initialValue) => {
        return initialValue
    }
}

makeCounter()